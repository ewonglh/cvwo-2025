// Displays thread content and comments

import type Thread from "../types/Thread";
import type Comment from "../types/Comment";

export default function ThreadView(thread: Thread, comments: Comment[]){ {
    return (
        <Container>
            
        </Container>
    );
}