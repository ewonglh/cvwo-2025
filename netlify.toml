[build]
  command = "npm run build"
  publish = "dist"

# Proxy /api requests to the backend server
# This makes requests appear to come from the same origin, 
# allowing HttpOnly cookies to work without complex CORS/SameSite issues.
[[redirects]]
  from = "/api/*"
  to = "https://cvwo-backend-0rmz.onrender.com" # TODO: Update this to your production backend URL
  status = 200
  force = true
